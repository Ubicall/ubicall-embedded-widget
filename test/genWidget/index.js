// var should = require("should");
// var fs = require("fs");
// var settings = require("../../settings");
// var genWidget = require("../../genWidget");
// var beautify_html = require("js-beautify").html;
//

describe("used to generate widget html file form plist one and save it to settings.cdn.widget", function() {

    var widget;

    before(function() {
        //widget = genWidget.generate("https://designer.ubicall.com/plist/29kwX5FfFDQP");
    });
});